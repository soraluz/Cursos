/* import { Usuarios } from "@/components/PublicData";
let i=5

export default function handler(req, res) {
    
    const input= req.body
    input.id_usuario=i++
    
    Usuarios.push(input)
    console.log(Usuarios)
    res.status(200).json(input);
    
} */